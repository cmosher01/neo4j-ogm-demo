apply plugin: JavaPlugin

group = 'nu.mine.mosher.neo4j'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
}

test {
    useJUnitPlatform()
    testLogging {
        showStandardStreams = true
        events "started", "passed", "skipped", "failed"
        exceptionFormat "full"
    }
}
